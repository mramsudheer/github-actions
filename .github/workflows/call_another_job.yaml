name: feature-pipeline
on: [push] # Triggers the workflow on every push to the repository
jobs:
  # Job name
  feature-pipeline:
    # 'uses' is nothing but shared library
    uses: mramsudheer/reusableWorkflows/.github/workflows/nodejs-feature-pipeline.yaml@main
    with:
      project: 'roboshop'
      component: 'catalogue'
      environment: 'dev' 
  