name: self Hosted
run-name: "Manual Run by @${{ github.actor }}"
#on: [push] # Triggers the workflow on every push to the repository
on: [workflow_dispatch] # Triggers the workflow manually


jobs:
  # Job name
  self-hosted:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Greet the world 
        run: echo "Hello, from self hosted runner!" # The command to execute